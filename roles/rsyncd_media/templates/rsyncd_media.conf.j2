gid = users
read only = true
use chroot = true
transfer logging = true
log format = %h %o %f %l %b
log file = /var/log/rsyncd.log
pid file = /var/run/rsyncd.pid

[files]
    path = {{ nginx.document_root }}
    comment = adesso fair files
    read only = false
    uid = webapp
